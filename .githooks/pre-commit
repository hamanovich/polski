#!/bin/sh
# Не пускаем в коммит файлы, которые не парсятся - иначе сайт молча отдаст пустую страницу.
for f in data.js app.js; do
  node --check "$f" || { echo "pre-commit: синтаксическая ошибка в $f, коммит отменён"; exit 1; }
done
