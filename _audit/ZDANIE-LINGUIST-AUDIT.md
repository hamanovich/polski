# Собери фразу: аудит банка, ярус 1

## 1. Область проверки

- Материал: 24 записи `zdanie` первого яруса в `game-data.js` и 2 новых правила в общей таблице `rules` (`order-klityki`, `order-sie`).
- Уровень: A1-A2, ярус «узнавание».
- Правила игры: `_games/ZDANIE.md`, разделы «Что считается верным» и «Лишние плитки».
- Дата: 2026-09-21.
- Охват: каждая запись проверена по четырём вопросам: нормативность всех фраз `answers`; полнота списка при данном условии; что ни одна лишняя плитка не складывается в другую верную фразу с тем же числом слов; точность объяснения. Для полноты списка скрипт перебрал все перестановки слов каждой записи, отбросил нарушения жёстких границ (клитика в начале, предлог без своего слова, `nie` не перед глаголом), остаток разобран вручную.
- Проход один: составление и вычитка сделаны одним составителем. Независимой вычитки без контекста составителя, как у Детектива, ещё не было.

## 2. Итог

Грубых языковых ошибок нет: все фразы `answers` нормативны, все лишние плитки дают ошибочные фразы. Главный риск игры, неполный список верных порядков, подтвердился на трёх записях: при переборе нашлись нейтральные порядки, которых в `answers` не было. Они добавлены. Ещё две записи исправлены: в одной объяснение обобщало сверх нормы, в другой лишняя плитка давала эллиптическое прочтение. Банк пригоден для первой версии движка; перед добором второго яруса нужна независимая вычитка трёх мягко принятых порядков (пункт 4).

## 3. Сводная таблица

| № | Запись | Тип | Приоритет | Статус |
|---|---|---|---|---|
| 1 | zd-wieczorem-slucham-radia, zd-wracam-do-domu-tramwajem, zd-znam-go-od-dawna | неполный список порядков | P1 | исправлено |
| 2 | zd-moi-rodzice-mieszkaja-na-wsi | опасное обобщение в объяснении, лишняя плитка не по теме | P1 | исправлено |
| 3 | zd-szefa-nie-ma | лишняя плитка даёт эллиптическое прочтение | P1 | исправлено |
| 4 | zd-jak-sie-czujesz и два порядка из пункта 1 | мягко принятые порядки | P2 | не проверено, оставлено |
| 5 | черновики до вычитки | непроверенные ошибки, совпадение с русским, второе прочтение | P1 | исправлено при составлении |

## 4. Подробно

**1. Неполный список порядков, P1.** Перебор перестановок показал нейтральные порядки, которых не было в `answers`; игрок, собравший их, получил бы «мимо» за верную фразу.
- `Słucham wieczorem radia.` Обстоятельство времени между глаголом и дополнением звучит так же нейтрально, как в начале и в конце.
- `Do domu wracam tramwajem.` Новое сообщение (`tramwajem`) стоит в конце, как требует раздел «Тема и новое сообщение»; при переводе «возвращаюсь домой на трамвае» акцент именно на способе.
- `Od dawna znam go.` Краткое `go` после глагола стоит в безударной позиции, что допускает правило Bańko (см. источники). Порядок менее частотен, чем `Od dawna go znam`, но ошибкой не является.

Статус: обоснованный вывод.

**2. zd-moi-rodzice-mieszkaja-na-wsi, P1.** Было: лишние плитки `moje` и `w`, объяснение заканчивалось фразой «В деревне по-польски na wsi». Это обобщение неверно: рядом с `na wsi` («в сельской местности») нормативно `we wsi` о конкретной деревне. Плитка `w` проверяла не мужско-личную форму, заявленную темой, а выбор предлога, и при этом давала `w wsi` - форму, неверную ещё и потому, что перед `wsi` предлог расширяется до `we`. Стало: одна лишняя плитка `moje`, объяснение только о мужско-личной форме. Статус: обоснованный вывод.

**3. zd-szefa-nie-ma, P1.** Было: лишние `szef` и `jest`. Из трёх плиток складывались `Szef nie ma` и `Nie jest szef`: эллиптические фразы («у начальника нет», «это не начальник»), которые отсекал только русский перевод. Кроме того, объяснение называло `szef nie jest` незаконченной фразой, а раздел `/negation/#~nie-ma-или-nie-jest` прямо признаёт `Ona nie jest w domu` верной фразой с другим смыслом. Стало: одна лишняя плитка `jest`, которая с тремя ячейками даёт только неверные `Szefa nie jest` и `Nie jest szefa`; объяснение пересказывает противопоставление из раздела. Статус: согласовано с проектом.

**4. Мягко принятые порядки, P2, не проверено.** Три фразы засчитываются, хотя их естественность не подтверждена источником: `Jak czujesz się?`, `Od dawna znam go.`, `Za zaproszenie dziękuję wam.` Принцип принятия: если порядок грамматичен и не требует особого ударения, лучше засчитать его, чем наказать честного игрока. Обратный риск - закрепить у учащегося менее частотный порядок - меньше, потому что разбор всегда показывает основную фразу `answers[0]` первой. Оценка «звучит менее гладко» про `Jak czujesz się?` из объяснения снята: подтвердить её нечем. Для независимой вычитки: решить, остаются ли эти три фразы в `answers`.

**5. Черновики, исправлены при составлении.**
- `Rozmawiamy przez telefon`, лишние `po`, `telefonie`: в Poradni PWN нет ответа, который подтверждал бы или опровергал `po telefonie`. По правилу из аудита Детектива непроверенная калька в банк не идёт.
- `Pytam go o drogę`, лишние `u`, `niego`: ошибочность `pytać u kogoś` не нашлась в прочитанных источниках. Не взято.
- `Pomagam babci w ogrodzie`, лишняя `babcię`: русское «помогаю бабушке» тоже с дательным, ошибка не типовая. Не взято.
- `Nikt tu nie mieszka`, лишняя `ktoś`: `Ktoś tu nie mieszka` - верная фраза с другим смыслом. Не взято.
- `Podoba mi się to miasto`: `Mi się podoba` в начале фразы распространено в речи, хотя Bańko считает начальную позицию краткой формы невозможной. Для первого яруса слишком спорно, отложено до яруса с явным условием.
- `Dziękuję ci za pomoc`: совпадает с вопросом Milionerzy. Заменено на `Dziękuję wam za zaproszenie`.
- `Jedziemy nad morze`: `na morze` грамматично в значении «в море, на лодке». Не взято, по тому же правилу, что в Детективе.

## 5. Проверено и признано корректным

Управление (`dziękować wam`, `szukać nowego mieszkania`, `słuchać radia`, `dzwonić do mamy`, `czekać na pociąg`, `prosić o ciszę`, `opiekować się psem`), родительный при отрицании (`mleka`, `tych ludzi`), краткие формы (`go`, `mi`, `mu`), `się` (`jak się czujesz`, `przy sobie`), предлоги (`do Gdańska`, `do szkoły`), творительный (`tramwajem`, `pielęgniarką`), мужско-личные формы (`moi rodzice`, `nasi goście przyszli`), род (`poważny problem`, `ten system prosty`), `po polsku`. Во всех записях проверены сборки с лишними плитками при том же числе ячеек: каждая даёт ошибочную фразу.

Контекст «Нейтрально, без выделения» стоит у записей, где лишняя ударная форма (`jego`, `mnie`, `jemu`) или вынос дополнения в начало дали бы верную фразу с противопоставлением. Запись о родителях получила вместо него вопрос: без него `Na wsi mieszkają moi rodzice` было бы равноправным ответом.

## 6. Изменено, отклонено, осталось

- Изменено: пункты 1-3 и 5.
- Отклонено: ничего.
- Осталось: независимая вычитка пункта 4; второй и третий ярусы.

## 7. Сборка и проверки

`npm run check` проходит. `check-build.mjs` получил проверки банка `zdanie`: префикс и уникальность `id`, ярус, одинаковый набор слов у всех фраз `answers` и различие их порядка, 1-3 лишние плитки, которые не совпадают со словами фразы, не больше 12 плиток, `names` среди плиток, правило и его якорь, `drill` и `drillKey`, 24 записи первого яруса. Новые правила `order-klityki` и `order-sie` ссылаются на существующие заголовки `/word-order/`.

## 8. Источники

- M. Bańko, «mnie czy mi? tobie czy ci?», Poradnia językowa PWN, https://sjp.pwn.pl/poradnia/haslo/mnie-czy-mi-tobie-czy-ci;5375.html - прочитано. Краткие формы `mi`, `go`, `ci` в начале фразы не используются, полные - только под ударением: «Nie widzę go», но «Jego nie widzę». Подтверждает пункт 1 (`Od dawna znam go`) и записи с `go`, `mi`, `mu`.
- WSJP PAN, «opiekować się», https://wsjp.pl/haslo/podglad/13572/opiekowac-sie - прочитано. Składnia `opiekować się + KIM/CZYM`, конструкций с `za` нет.
- WSJP PAN, «czuć się», https://wsjp.pl/haslo/podglad/32354/czuc-sie/4965412/bezpiecznie - прочитано. Примеров вопроса `jak się czujesz` в статье нет, поэтому порядок в пункте 4 остаётся не проверенным.
- Разделы проекта: `/word-order/` (тема и новое сообщение, клитики, позиция się), `/negation/#~nie-ma-или-nie-jest`, `/verbs/#rekcja` (таблица `REKCJA`), `/gender/#~род-расходится-с-русским`.
- Poradnia PWN, поиск по «po telefonie» и «pytać u» - результатов, относящихся к делу, нет; в доказательства не пошло.

# Ярус 2: применение правила

## 1. Область проверки

- Материал: 24 записи второго яруса и новое правило `order-tema-rema`.
- Уровень: A2-B1. Темы: тема и рема с вопросом в условии, время, мужско-личные формы, числительные 2-4 и 5+, отрицание, управление, клитики `ci się`.
- Дата: 2026-09-21.
- Охват: тот же, что для первого яруса. К перебору перестановок добавлен перебор подстановок: каждая лишняя плитка ставится на место каждого слова каждой фразы `answers`, чтобы поймать лишнюю плитку, которая складывается в верную фразу.
- Проход один, независимой вычитки не было.

## 2. Итог

Языковых ошибок нет, ни одна подстановка лишней плитки не даёт верной фразы. Перебор снова нашёл нейтральные порядки, которых не было в `answers`; они добавлены. Записи, где верен только порядок «известное, потом новое» (`Tort upiekła babcia`, `Dzwonił do ciebie Marek`), получили в условии уточнение «без выделения голосом»: без него прямой порядок с логическим ударением на подлежащем тоже был бы верным ответом.

## 3. Сводная таблица

| № | Запись | Тип | Приоритет | Статус |
|---|---|---|---|---|
| 1 | zd-czy-podoba-ci-sie-krakow, zd-pieciu-kolegow-czekalo, zd-nie-ma-tu-nikogo, zd-nie-widze-tu-zadnego-sklepu, zd-wczoraj-nie-bylo-pradu, zd-lekcja-zaczyna-sie-o-osmej | неполный список порядков | P1 | исправлено |
| 2 | zd-tort-upiekla-babcia, zd-dzwonil-do-ciebie-marek | порядок решает смысл только без логического ударения | P1 | исправлено при составлении |
| 3 | zd-ci-chlopcy-graja-w-pilke | перестановка даёт фразу с другим смыслом | P2 | оставлено |
| 4 | черновики до вычитки | вторая модель, неподтверждённая ошибка | P1 | исправлено при составлении |

## 4. Подробно

**1. Неполный список порядков, P1.** Добавлены `Czy ci się Kraków podoba?` (клитики сразу после `czy`, подлежащее перед глаголом), `Pięciu kolegów na mnie czekało.`, `Tu nikogo nie ma.`, `Tu żadnego sklepu nie widzę.`, `Nie było prądu wczoraj.`, `Prądu nie było wczoraj.`, `Zaczyna się lekcja o ósmej.` Последние три засчитываются мягко: в каждом новое (время) стоит в конце, и порядок грамматичен, но заметно реже основного. Статус: обоснованный вывод.

**2. Тема и рема без ударения, P1.** Раздел `/word-order/#~тема-и-новое-сообщение` сам называет правило тенденцией: «контрастное ударение может выделить почти любое слово». `Babcia upiekła tort` с ударением на `babcia` - верный ответ на «кто испёк торт». Условие «без выделения голосом» закрывает это прочтение и делает задание честным. Статус: согласовано с проектом.

**3. zd-ci-chlopcy-graja-w-pilke, P2, оставлено.** Перестановка `Chłopcy ci grają w piłkę` грамматична: `ci` читается как дательный «тебе». Смысл («мальчики играют тебе в мяч») расходится с переводом «Эти мальчики играют в мяч», и собрать такую фразу, прочитав задание, нельзя. Оставлено; если вычитка сочтёт иначе, замена `ci chłopcy` на `ci panowie` снимает вопрос.

**4. Черновики, исправлены при составлении.**
- `Pokażę ci go jutro` с лишними `jego`, `tobie`: неясно, допустим ли порядок `pokażę go ci`, а проверить его не удалось. Не взято.
- `Pytam go o drogę`, `Zapytam szefa o urlop` с лишней `u`: ошибочность `pytać u kogoś` по-прежнему не подтверждена. Не взято.
- `Czego szukasz?` с лишней `co`: `co szukasz` встречается в разговорной речи. Не взято.
- `Nikt mi nie pomógł`: русское «никто не помог» устроено так же, ошибка не типовая. Не взято.
- `Pociąg przyjeżdża o ósmej`: два задания про поезд в одном ярусе. Заменено на `Lekcja zaczyna się o ósmej`.

## 5. Проверено и признано корректным

`na biurku` на вопрос «где», `jest mleko` при утвердительном `jest`, `upiekła` с подлежащим женского рода, `o ósmej`, `za kim tęsknisz`, `ci się` без выделения, `żadnego sklepu` и `nie było prądu` при отрицании, `nikogo` в `nie ma`, `ci chłopcy`, `Anna i Piotr byli`, `nasi sąsiedzi są mili`, `jest pięć kobiet`, `stoją cztery krzesła`, `pięciu kolegów czekało`, `gratuluję ci nowej pracy`, `potrzebuję twojej pomocy`, `używamy tego programu`, `za dziesięć minut`, `ze szkoły`, `był dobrym lekarzem`, `nową sukienkę`, `dzwonił do ciebie`, `o co pytasz`.

Пары лишних плиток проверены вместе: `nowa` + `sukienka`, `żaden` + `sklep`, `krzeseł` + `stoi`, `czekali` + `pięć` дают только ошибочные фразы. У записи с числительными `dwóch` в лишние плитки не ставится: раздел `/numerals/#~мужско-личные-формы` признаёт `Dwóch studentów czyta` равноправным вариантом.

# Ярус 3: свободное владение

## 1. Область проверки

- Материал: 24 записи третьего яруса и 6 новых правил: `order-przymiotnik`, `ktory-przypadek`, `ze-zeby`, `gdyby-warunek`, `by-ruchome`, `pytanie-posrednie`.
- Уровень: B1-B2. Темы: придаточные с `żeby`, `który`, `czy`, подвижное `by`, `gdyby`, собирательные числительные, мужско-личное `dziesięciu`, порядок прилагательного, тема и рема, управление.
- Дата: 2026-09-21.
- Охват: тот же. Для сложных предложений перестановки мало что дают, поэтому основной перебор - подстановки, включая две лишние плитки сразу: так складываются пары вроде `że` + `wrócę`.

## 2. Итог

Языковых ошибок нет. Подстановки нашли одну опасную лишнюю плитку: `siebie` в записи `zd-nie-chce-zebys-sie-martwil` давала маргинально грамматичную фразу. Плитка снята. Остальные пары лишних плиток дают только ошибочные фразы.

## 3. Сводная таблица

| № | Запись | Тип | Приоритет | Статус |
|---|---|---|---|---|
| 1 | zd-nie-chce-zebys-sie-martwil | лишняя плитка складывается в грамматичную фразу | P1 | исправлено |
| 2 | придаточные с `żeby` | лишняя `żeby` давала верную фразу о третьем лице | P1 | исправлено при составлении |
| 3 | сравнение с `od` | лишняя `niż` давала верную фразу | P1 | исправлено при составлении |
| 4 | zd-chetnie-bym-z-wami-pojechal | возможный пропущенный порядок | P2 | не проверено, оставлено |
| 5 | черновики до вычитки | тип прилагательного, неподтверждённые ошибки | P1 | исправлено при составлении |

## 4. Подробно

**1. zd-nie-chce-zebys-sie-martwil, P1.** Было: лишние `martwiłeś` и `siebie`. С `siebie` вместо `się` складывается `Nie chcę, żebyś siebie martwił`: `martwić kogoś` - переходный глагол («огорчать»), и ударное `siebie` при нём грамматично. Стало: одна лишняя плитка `martwiłeś`, объяснение больше не говорит о «себя».

**2. Лишняя `żeby`, P1.** В черновике `Mama chce, żebym wrócił wcześniej` лишней плиткой стояла `żeby`. С ней складывается `Mama chce, żeby wrócił wcześniej` - верная фраза о третьем лице («чтобы он вернулся»), которую отсекал только перевод. Та же ловушка была у `gdyby` при `gdybym wiedział`. Лишними стали `że` и форма будущего (`wrócę`), `jeśli` и `wiedziałem`: с ними верной фразы не собрать.

**3. Сравнение, P1.** Черновик `Ona jest wyższa od siostry` с лишней `niż`: `Ona jest wyższa niż siostry` читается как «выше, чем сёстры», это верная фраза. Запись не взята.

**4. zd-chetnie-bym-z-wami-pojechal, P2, не проверено.** Порядок `Chętnie z wami bym pojechał` (частица после предложной группы) грамматичен по правилу `by-ruchome`, но звучит реже, чем `chętnie bym`. В `answers` его нет. Вычитке решить, добавлять ли.

**5. Черновики, исправлены при составлении.**
- Порядок прилагательного: `bilet miesięczny`, `egzamin ustny`, `woda gazowana` не взяты, потому что препозиция (`miesięczny bilet`) встречается в речи. Оставлено одно задание на лексикализованное `piłka nożna`, где препозиция невозможна.
- `Nie wiem, gdzie mieszka Ola` с лишней `czy`: `Nie wiem, czy mieszka Ola` - верная фраза. Не взято.
- `Kawa jest droższa od herbaty` с лишней `niż`: та же ловушка, что в пункте 3.
- `To jest dziewczyna, o której ci mówiłem` с лишней `którą`: ошибка `o którą` не типовая. Заменено на `To jest problem, o którym mówiłem` с лишней `której`: род `problem` расходится с русским, это подтверждено в `ROD_DIFF`.

## 5. Проверено и признано корректным

`żebym wrócił`, `żeby zamknął`, `żeby kupić`, `żeby nie obudzić dziecka` (родительный при отрицании инфинитива), `żebyś się martwił`; `której szukam`, `którzy mi pomogli`, `który kupiliśmy` (неодушевлённый винительный), `o którym`; `chętnie bym pojechał`, `gdzie byś chciał`, `ja bym tak nie powiedział`, `gdybym wiedział, powiedziałbym`; `troje dzieci bawiło się`, `przyszło dziesięciu pracowników`; `piłka nożna`; `«Pana Tadeusza» napisał`; `że go znasz`; `śmiać się z`; `chorować na serce`; `wolnych miejsc`; `zapytaj, czy`; `wygrała nasza drużyna`; `która jest godzina`.

Перестановка частей сложного предложения засчитывается там, где она нейтральна: `Żeby kupić mieszkanie, oszczędzam`, `Powiedziałbym ci, gdybym wiedział`. В остальных записях главная часть стоит первой, и перенос придаточного вперёд его выделяет.

## 6. Изменено, отклонено, осталось

- Изменено: пункт 1 третьего яруса, пункты 1-2 второго.
- Отклонено: ничего.
- Осталось: независимая вычитка всех трёх ярусов; мягко принятые порядки (ярус 1, пункт 4; ярус 2, пункт 1; ярус 3, пункт 4); `ci chłopcy` (ярус 2, пункт 3).

## 7. Сборка и проверки

`npm run check` проходит, размеры ярусов 24 + 24 + 24 заперты в `check-build.mjs`. Все `drillKey` - настоящие ключи колод. Моделирование сборки партии (5000 партий по правилам движка) дало 0 неполных партий, в партиях встречаются все 72 записи.

## 8. Источники

- WSJP PAN, «chorować (na grypę)», https://wsjp.pl/haslo/podglad/31571/chorowac/3883857/na-grype - прочитано. Składnia `chorować + na CO`, в цитатах «chorował na serce».
- M. Bańko, «tytuły filmów», Poradnia językowa PWN, https://sjp.pwn.pl/poradnia/haslo/tytuly-filmow;4029.html - прочитано. «Czytam Pana Tadeusza»: названия книг склоняются.
- M. Bańko, «mnie czy mi? tobie czy ci?», см. источники первого яруса: безударные формы после союза и `czy`.
- Разделы проекта: `/conjunctions/#~ze-или-zeby` и `#~косвенная-речь`, `/questions/#~ktory-как-относительное-местоимение` и `#~прямои-и-косвенныи-вопрос`, `/verbs/#tryby/~куда-прилипает-by`, `/numerals/#~мужско-личные-формы` и `#~собирательные`, `/adjectives/#~подводные-камни`, `/word-order/`.

# Независимая вычитка

## 1. Область проверки

- Материал: все 72 записи `zdanie` и таблица `rules`.
- Проверяющий: отдельный агент без контекста составителя. Он читал `LINGUIST.md`, `_games/ZDANIE.md` и банк, а этот аудит не открывал.
- Дата: 2026-09-21.
- Принцип, заданный вычитке: засчитывается любой грамматичный порядок, в том числе редкий и книжный; не засчитываются только порядки, чей смысл расходится с условием записи.

## 2. Итог

Вычитка подтвердила главный риск игры: списки `answers` были неполны, сильнее всего в записях без условия. 43 записи признаны чистыми. Все находки по полноте приняты, кроме тех, что противоречили правилу о лишних плитках. Две лишние плитки складывались в грамматичные фразы; одна запись заменена целиком.

## 3. Сводная таблица

| № | Запись | Находка | Решение |
|---|---|---|---|
| 1 | zd-ksiazka-ktorej-szukam, zd-ludzie-ktorzy-mi-pomogli, zd-samochod-ktory-kupilismy, zd-problem-o-ktorym-mowilem | нет книжной инверсии `Jest to ... / Są to ...` | добавлено |
| 2 | zd-zeby-nie-obudzic-dziecka | нет `Ciszej mów` и выноса придаточного | добавлены 4 порядка |
| 3 | zd-poprosilem-go-zeby-zamknal, zd-oszczedzam-zeby-kupic | нет дополнения перед глаголом в придаточном | добавлено |
| 4 | zd-nasi-goscie-juz-przyszli | нет `Przyszli już nasi goście`, `Już nasi goście przyszli` | добавлено, вместе с `Już przyszli goście nasi` |
| 5 | zd-w-grupie-jest-piec-kobiet | ответ на «сколько» естественнее с числом в конце: `Kobiet w grupie jest pięć` | добавлены 3 порядка |
| 6 | zd-nie-mamy-wolnych-miejsc | нет `Już wolnych miejsc nie mamy` | добавлено, вместе с `Wolnych miejsc nie mamy już` |
| 7 | zd-tort-upiekla-babcia | лишняя `babci` давала `Upiekła babci tort` | плитка снята; добавлено `Upiekła tort babcia` |
| 8 | zd-pana-tadeusza-napisal-mickiewicz | лишняя `Pan` давала `napisał pan Mickiewicz` | запись заменена, см. ниже |
| 9 | 6 записей с условием «нейтрально» | вынос обстоятельства в начало принимался выборочно | принимается везде, где новое остаётся в конце: `Tramwajem wracam do domu`, `Przy sobie mam paszport`, `Paszport mam przy sobie`, `Na mnie czekało pięciu kolegów`, `O ciszę prosimy`, `Do Gdańska jedziemy jutro`, `Do mamy dzwonię codziennie` |
| 10 | zd-pociag-odjezdza-za-dziesiec-minut, zd-w-parku-bawilo-sie-troje-dzieci, zd-ja-bym-tak-nie-powiedzial, zd-wygrala-nasza-druzyna | порядки, которые соседние записи уже принимают | добавлено |
| 11 | zd-telefon-lezy-na-biurku | пример `połóż na biurko` учил бы, что `połóż na biurku` - ошибка, хотя местный падеж при kłaść частотнее | пример заменён на `telefon spadł na biurko` |
| 12 | zd-uzywamy-tego-programu | подсказка «пользуемся программой» толкала к творительному, а объяснение говорило о «используем» | подсказка: «Мы каждый день используем эту программу» |
| 13 | zd-ewa-kupila-nowa-sukienke | типовая ошибка при русском «новое платье» - `nowe`, а не `nowa` | лишняя плитка `nowe`, объяснение дополнено |
| 14 | zd-wczoraj-nie-bylo-pradu | `nie był prąd` подано как готовая фраза | объяснение: личное być без именной части фразы не даёт |
| 15 | zd-gdzie-bys-chcial-pojechac | стоит назвать `dokąd` | дописано |
| 16 | zd-dziekuje-wam-za-zaproszenie | граница «без выделения» размыта | оставлено, по заключению вычитки |
| 17 | zd-o-co-pytasz | не проверено, ненормативно ли `pytać o czym` | проверено: WSJP даёт `zapytać KOGO o KOGO/CO`, модели с предложным нет; плитка `czym` честная |

## 4. Подробно

**8. Замена записи.** Вычитка предлагала оставить лишнюю плитку `Tadeusz`. Тогда складывается `«Pana Tadeusza» napisał Tadeusz Mickiewicz`: фраза фактически неверна, но грамматична, а правило лишних плиток запрещает любые грамматичные сборки. Запись заменена на `«Lalkę» napisał Bolesław Prus` с лишней `Lalka`. Тема та же - тема и рема, склонение названия в кавычках, - но из `Lalka` грамматичной фразы не собрать: `«Lalka» napisał Bolesław Prus` ошибочна. Подстановки перепроверены.

**Не принято:** вычитка сама отвела `tu` в конце фразы (`Nie ma nikogo tu`) и `Nie znam ludzi tych` как сомнительные. Составитель согласен, в `answers` их нет.

## 5. Источники

- WSJP PAN, «zapytać», https://wsjp.pl/haslo/podglad/33012/zapytac - прочитано. Składnia `zapytać + KOGO + (o KOGO/CO)`, коллокации `o adres, o cenę, o drogę`; модели `o + Miejscownik` нет.
- Poradnia PWN, «kłaść / położyć», https://sjp.pwn.pl/poradnia/haslo/klasc-polozyc;19369.html - прочитано вычиткой. При kłaść верны обе конструкции, местный падеж частотнее.
- Poradnia PWN, «zaimki gdzie i dokąd», https://sjp.pwn.pl/poradnia/haslo/zaimki-gdzie-i-dokad;23337.html - прочитано вычиткой. Оба местоимения нейтральны, dokąd специализирован на направлении.
